# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=psx_stocks
DB_USER=ahmednaeem
DB_PASSWORD=password
# SSL Configuration (required for remote databases like Aiven)
# Set to 'true' for cloud databases (Aiven, AWS RDS, etc.)
DB_SSL=true
# Set to 'false' for self-signed certificates (cloud databases)
DB_SSL_REJECT_UNAUTHORIZED=false

# Scraping Configuration
SCRAPE_DELAY_MIN=2000
SCRAPE_DELAY_MAX=5000
HEADLESS=true
PSX_SCREENER_URL=https://dps.psx.com.pk/screener

# Analysis Configuration
SHORT_TERM_MONTHS=6
MEDIUM_TERM_MONTHS=18
TOP_STOCKS_LIMIT=10

# Email Configuration
EMAIL_SERVICE=gmail
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=mailerhere10@gmail.com
EMAIL_APP_PASSWORD="gxib hifn amfv bhcc"
EMAIL_FROM=PSX Stock Analysis

# Scheduler Configuration
SCHEDULER_ENABLED=true
SCHEDULER_TIMEZONE=Asia/Karachi
SCHEDULER_CRON=0 6 * * *

# User Configuration
USER_EMAIL=ahmednaeem.career@gmail.com
USER_ID=default

# API Configuration (optional for future use)
API_PORT=3000
